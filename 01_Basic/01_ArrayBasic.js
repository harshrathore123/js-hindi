console.log("Array Practical Quesiton Based on Methods:")

const myarr = [1,4,23,3,42,1]
console.log(myarr);

//Method Splice:

const newArr = myarr.splice(1,2);
console.log(newArr);

//So from splice method we can understand that if we are using splicing method then it removing that element 
//which we pass in splice method from our original array
console.log("Original Array:",myarr);